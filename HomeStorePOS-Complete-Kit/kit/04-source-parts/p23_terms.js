/* ============================================================
   TERMS & CONDITIONS, SIGNATURE PAGE, CUSTOMER ID
   ============================================================ */
function printHeader(st,title){ return `<div style="display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:10px"><div>${typeof logoHTML==="function"?logoHTML(56):""}<h1 style="margin:0">${esc(st.name)}</h1><div>${esc(st.address||"")}${st.phone?" · "+esc(st.phone):""}</div></div><div style="text-align:right"><h2 style="margin:0">${esc(title)}</h2><div>${dateFmt(todayISO())}</div></div></div>`; }
const DEFAULT_TERMS={version:"1",
 general:"All merchandise is sold subject to the terms below. Prices, taxes and fees are as shown on this invoice. A deposit is required to place an order; the balance is due before merchandise is released, delivered or picked up unless a financing or lease agreement is in place. Merchandise remains the property of the store until paid in full.",
 returns:"Stock merchandise in original, unused condition may be returned or exchanged within 7 days of receipt with this invoice; a restocking fee may apply. Mattresses and bedding are subject to the comfort-trial terms shown on this invoice and require a store-approved protector. Clearance, floor-sample and as-is items are final sale.",
 special:"Special-order and custom-ordered merchandise is ordered specifically for you and cannot be cancelled, returned or exchanged once the order has been placed with the manufacturer. Estimated arrival dates are provided by the manufacturer and are not guaranteed; the store will keep you informed of changes.",
 delivery:"Delivery is to the address on this invoice. Please ensure a clear path and adequate space; the store is not responsible for damage to property caused by items that do not fit. Someone 18 or older must be present to inspect merchandise and sign the receipt of goods. Any damage or missing items must be noted on the receipt of goods at the time of delivery. Merchandise held for pickup or delivery must be received within 30 days of notification that it is ready; storage fees may apply thereafter.",
 pickup:"Customers picking up merchandise are responsible for inspecting it before leaving and for loading and securing it in their vehicle. The store is not responsible for damage that occurs during transport by the customer.",
 financing:"For financed or leased items, your agreement with the finance or lease company governs payment terms, interest and fees. The store's return policy applies to the merchandise; refunds for financed or leased purchases are issued to the finance or lease company.",
 asIs:"Items marked FLOOR SAMPLE or AS-IS have been inspected and are accepted in their current condition. AS-IS items carry no warranty and are not returnable or exchangeable.",
 warranty:"Manufacturer warranties, where applicable, are provided by the manufacturer and are subject to its terms. The store will assist with claims but does not extend warranties beyond those of the manufacturer or of a protection plan purchased separately.",
 receipt:"I confirm that the items listed were delivered to the address above, that I inspected them at the time of delivery, and that they were received in acceptable condition except as noted on this receipt. I understand that damage not noted at delivery may not be covered.",
 custom:"",signaturePage:true,initials:true};
function termsCfg(){ const st=storeSettings(); return Object.assign({},DEFAULT_TERMS,st.terms||{}); }
function termsSections(inv){ const t=termsCfg(); const s=[["General terms",t.general],["Returns & exchanges",t.returns],["Special orders",t.special]]; if(!inv||inv.delivery) s.push(["Delivery",t.delivery]); if(!inv||inv.fulfillment==="pickup"||inv.fulfillment==="now") s.push(["Pickup",t.pickup]); if(!inv||inv.terms==="financing"||inv.terms==="lease"||(inv.lines||[]).some(l=>l.fundingId)) s.push(["Financing & lease-to-own",t.financing]); if(!inv||(inv.lines||[]).some(l=>l.asIs||l.floorSample)) s.push(["Floor sample / as-is",t.asIs]); s.push(["Warranty",t.warranty]); if(t.custom&&t.custom.trim()) s.push(["Store policies",t.custom]); return s.filter(x=>x[1]&&x[1].trim()); }
function termsHTML(inv){ return `<div style="margin-top:18px;border-top:1px solid #999;padding-top:8px"><h3 style="margin:0 0 6px;font-size:12px">Terms &amp; conditions</h3>${termsSections(inv).map(([h,txt])=>`<p style="font-size:10px;margin:0 0 5px"><b>${esc(h)}.</b> ${esc(txt)}</p>`).join("")}</div>`; }
function signaturePageHTML(inv){ const t=termsCfg(); const c=byId("customers",inv.customerId); const st=storeSettings(); return `<div class="page"><h1 style="font-size:20px">Customer acknowledgment</h1><p>${esc(st.name)} · Invoice ${esc(inv.number)} · ${dateFmt(inv.date)}</p><p><b>${esc(custDisplay(c))}</b><br>${esc(c?.address||"")}<br>${esc(c?.phone||"")}</p><table class="ptable"><tr><th>Qty</th><th>Item</th><th class="n">Amount</th></tr>${inv.lines.map(l=>`<tr><td>${l.qty}</td><td>${esc(l.name)}${l.special?" (ordered from supplier)":""}${l.asIs?" (AS-IS)":""}${l.floorSample?" (floor sample)":""}</td><td class="n">${money(l.price*l.qty)}</td></tr>`).join("")}</table><p style="margin-top:14px;font-size:12px">I have read and agree to the terms and conditions of this purchase, summarized below and printed in full on my invoice.</p>${termsSections(inv).map(([h,txt],i)=>`<div style="display:flex;gap:12px;align-items:flex-start;margin:8px 0"><div style="width:70px;flex:none;border-bottom:1px solid #000;font-size:${inv.signedInitials&&inv.signedInitials[i]?"13px;font-weight:700":"10px"};padding-top:${inv.signedInitials&&inv.signedInitials[i]?"8px":"14px"};text-align:center">${inv.signedInitials&&inv.signedInitials[i]?esc(inv.signedInitials[i]):(t.initials?"initials":"")}</div><div style="font-size:11px"><b>${esc(h)}.</b> ${esc(txt)}</div></div>`).join("")}<div style="display:flex;gap:30px;margin-top:26px;align-items:flex-end"><div style="flex:1">${inv.signature?`<img src="${inv.signature}" style="height:70px">`:`<div style="height:70px"></div>`}<div style="border-top:1px solid #000;padding-top:4px;font-size:11px">Customer signature — ${esc(inv.signedName||custDisplay(c))}${inv.signedAt?" · signed "+dateFmt(inv.signedAt):""}</div></div><div style="flex:1"><div style="height:70px"></div><div style="border-top:1px solid #000;padding-top:4px;font-size:11px">For the store — ${esc(staffName(inv.salesId))}</div></div></div>${c&&c.idPhoto&&termsCfg().idOnSignature?`<p style="font-size:10px;margin-top:10px">ID on file ✓</p>`:""}</div>`; }
function invoiceSign(id){ const inv=byId("invoices",id); const c=byId("customers",inv.customerId); customerModal(`<h2>Customer signature — ${esc(inv.number)}</h2><p class="muted small">${esc(custDisplay(c))} · by signing, the customer agrees to the terms &amp; conditions printed on the invoice.</p><div style="max-height:220px;overflow:auto;border:1px solid var(--line);border-radius:8px;padding:8px;margin-bottom:8px">${termsSections(inv).map(([h,txt],i)=>`<div style="display:flex;gap:8px;align-items:flex-start;margin:0 0 6px">${termsCfg().initials?`<input type="text" id="is_init_${i}" maxlength="4" placeholder="Initials" style="width:70px;flex:none;text-align:center;text-transform:uppercase;padding:4px">`:""}<p class="small" style="margin:0"><b>${esc(h)}.</b> ${esc(txt)}</p></div>`).join("")}</div><div class="field"><label class="f">Signed by</label><input type="text" id="sg_name" value="${esc(inv.signedName||custDisplay(c))}"></div><canvas class="sig" id="sigpad"></canvas><div class="row" style="margin:6px 0 10px"><button class="btn s fx" onclick="sigClear()">Clear</button><span class="muted small">Sign with a finger or mouse</span></div><div class="actions"><button class="btn" onclick="closeModal()">Cancel</button><button class="btn p" onclick="(async()=>{ if(sigEmpty) return toast('Please sign first.',true); const inv=byId('invoices','${id}'); if(termsCfg().initials){ const ini={}; const n=termsSections(inv).length; for(let i=0;i<n;i++){ const v=(fv('is_init_'+i)||'').trim().toUpperCase(); if(!v) return toast('Please initial every policy.',true); ini[i]=v; } inv.signedInitials=ini; } inv.signature=await storeImage('signatures',inv.id,document.getElementById('sigpad').toDataURL('image/png')); inv.signedAt=todayISO(); inv.signedName=fv('sg_name').trim(); inv.termsVersion=termsCfg().version; await save('invoices',inv); closeModal(); render(); toast('Signature saved on '+inv.number); })()">Save signature</button></div>`,true); setTimeout(sigInit,50); }
async function saveDocStyle(){ const st=storeSettings(); st.docStyle={style:fv("ds_style")||"modern",accent:(fv("ds_accent_t")||fv("ds_accent")||"#37A598").trim(),showLogo:fchk("ds_logo"),footerNote:fv("ds_foot").trim()}; st.printMode=fv("ds_print")||"preview"; st.custCopy={supplier:fv("cc_sup")||"code",hideSku:fchk("cc_sku"),hideModel:fchk("cc_model"),hideCollection:fchk("cc_coll")}; await save("settings",st); toast("Invoice design saved"); }
function renderTermsSettings(){ const t=termsCfg(); const f=(k,l,rows)=>`<div class="field"><label class="f">${l}</label><textarea id="tm_${k}" rows="${rows||3}">${esc(t[k]||"")}</textarea></div>`; const s=storeSettings(); return `<div class="card" style="max-width:860px"><h2>Invoice design</h2><div class="grid g3"><div class="field"><label class="f">Style</label><select id="ds_style"><option value="modern" ${(s.docStyle?.style||"modern")==="modern"?"selected":""}>Modern — colored header band</option><option value="classic" ${s.docStyle?.style==="classic"?"selected":""}>Classic — colored rule &amp; headings</option><option value="minimal" ${s.docStyle?.style==="minimal"?"selected":""}>Minimal — black &amp; white</option></select></div><div class="field"><label class="f">Accent color</label><div class="row" style="gap:6px;flex-wrap:nowrap"><input type="color" id="ds_accent" value="${esc(s.docStyle?.accent||currentPalette().accent||"#37A598")}" style="width:52px;height:38px;padding:2px" oninput="document.getElementById('ds_accent_t').value=this.value"><input type="text" id="ds_accent_t" value="${esc(s.docStyle?.accent||currentPalette().accent||"#37A598")}" oninput="document.getElementById('ds_accent').value=this.value" style="max-width:110px"><button class="btn s" onclick="const p=currentPalette();document.getElementById('ds_accent').value=p.accent;document.getElementById('ds_accent_t').value=p.accent">Theme color</button></div></div><div class="chk" style="margin-top:22px"><input type="checkbox" id="ds_logo" ${s.docStyle?.showLogo!==false?"checked":""}><label for="ds_logo">Show logo on documents</label></div></div><div class="field"><label class="f">Footer note on every invoice (optional)</label><input type="text" id="ds_foot" value="${esc(s.docStyle?.footerNote||"")}" placeholder="e.g. Thank you for shopping local — we appreciate your business!"></div><h3 style="margin:14px 0 6px">Customer copies</h3><div class="grid g3"><div class="field"><label class="f">Supplier shown on customer documents</label><select id="cc_sup"><option value="code" ${(s.custCopy?.supplier||"code")==="code"?"selected":""}>Short code only (set per supplier, e.g. ASH)</option><option value="none" ${s.custCopy?.supplier==="none"?"selected":""}>Not shown</option><option value="name" ${s.custCopy?.supplier==="name"?"selected":""}>Full supplier name</option></select></div><div class="chk" style="margin-top:22px"><input type="checkbox" id="cc_sku" ${s.custCopy?.hideSku?"checked":""}><label for="cc_sku">Hide SKUs on customer copies</label></div><div class="chk" style="margin-top:22px"><input type="checkbox" id="cc_model" ${s.custCopy?.hideModel?"checked":""}><label for="cc_model">Hide model / vendor item numbers</label></div></div><div class="chk"><input type="checkbox" id="cc_coll" ${s.custCopy?.hideCollection?"checked":""}><label for="cc_coll">Hide collection / series names</label></div><p class="muted small">These apply to invoices, quotes and pickup/delivery paperwork the customer receives. Purchase orders and internal pick lists always show full details. Any invoice can also be printed as a customer copy without prices from its <b>Customer copy</b> menu.</p><div class="field" style="max-width:320px"><label class="f">When printing</label><select id="ds_print"><option value="preview" ${(s.printMode||"preview")==="preview"?"selected":""}>Show a preview inside the app (recommended)</option><option value="tab" ${s.printMode==="tab"?"selected":""}>Open in a new browser tab</option></select></div><div class="row" style="margin-bottom:6px"><button class="btn p" onclick="saveDocStyle()">Save design</button><button class="btn" onclick="saveDocStyle().then(()=>{ const inv=S.invoices.find(i=>['paid','open','financed'].includes(i.status))||S.invoices[0]; if(inv) printInvoice(inv); else toast('Make a sale first to preview'); })">Preview a printed invoice</button></div></div><div class="card" style="max-width:860px"><h2>Terms &amp; conditions</h2><p class="muted small">Printed on every invoice and, with the signature page, presented for the customer's initials and signature. The wording below is a starting point — have your attorney review it for your state. Change the version number when you edit it so signed invoices record which terms they accepted.</p><div class="grid g3"><div class="field"><label class="f">Terms version</label><input type="text" id="tm_version" value="${esc(t.version||"1")}"></div><div class="chk" style="margin-top:22px"><input type="checkbox" id="tm_sigpage" ${t.signaturePage?"checked":""}><label for="tm_sigpage">Include the acknowledgment &amp; signature page when printing invoices</label></div><div class="chk" style="margin-top:22px"><input type="checkbox" id="tm_initials" ${t.initials?"checked":""}><label for="tm_initials">Initials line beside each policy</label></div></div>${f("general","General terms")}${f("returns","Returns &amp; exchanges")}${f("special","Special orders")}${f("delivery","Delivery")}${f("pickup","Pickup")}${f("financing","Financing &amp; lease-to-own")}${f("asIs","Floor sample / as-is")}${f("warranty","Warranty")}${f("receipt","Receipt of goods (signed by the customer at delivery)")}${f("custom","Your own additional policies (printed as “Store policies”)",4)}<div class="field"><label class="f">Invoice footer line (thank-you, hours, website)</label><textarea id="st_foot" rows="2">${esc(storeSettings().footer||"")}</textarea></div><div class="row"><button class="btn p" onclick="saveTerms()">Save terms</button><button class="btn" onclick="(async()=>{if(confirm('Reset all wording to the defaults?')){const st=storeSettings();st.terms={version:termsCfg().version};await save('settings',st);render()}})()">Reset to defaults</button><button class="btn" onclick="previewTerms()">Preview signature page</button></div></div>`; }
async function saveTerms(){ const st=storeSettings(); const t={}; for(const k of ["version","general","returns","special","delivery","pickup","financing","asIs","warranty","receipt","custom"]) t[k]=fv("tm_"+k); t.signaturePage=fchk("tm_sigpage"); t.initials=fchk("tm_initials"); st.terms=t; if(document.getElementById("st_foot")) st.footer=fv("st_foot"); await save("settings",st); toast("Terms saved"); }
function previewTerms(){ const inv=S.invoices.find(i=>i.lines&&i.lines.length)||{number:"INV-0000",date:todayISO(),customerId:S.customers[0]?.id||"",lines:[{qty:1,name:"Sample sofa",price:999}],delivery:true,salesId:session.staffId}; printHTML(signaturePageHTML(inv),"Signature page preview"); }
/* ---- customer ID photo & AAMVA barcode ---- */
function parseAAMVA(txt){ txt=String(txt||""); if(!/DAQ|DCS|DAG/.test(txt)) return null; const get=k=>{ const m=txt.match(new RegExp("(?:^|\\n|\\r)"+k+"([^\\n\\r]*)")); return m?m[1].trim():""; }; const first=get("DAC")||get("DCT").split(/[ ,]/)[0]||""; const last=get("DCS"); const mid=get("DAD"); const street=get("DAG"); const city=get("DAI"); const state=get("DAJ"); const zip=(get("DAK")||"").replace(/[^0-9-]/g,"").slice(0,5); const dobRaw=get("DBB"); const dob=dobRaw?(/^\d{8}$/.test(dobRaw)?(dobRaw.length===8&&+dobRaw.slice(0,2)<=12?dobRaw.slice(4,8)+"-"+dobRaw.slice(0,2)+"-"+dobRaw.slice(2,4):dobRaw.slice(0,4)+"-"+dobRaw.slice(4,6)+"-"+dobRaw.slice(6,8)):dobRaw):""; const exp=get("DBA"); const lic=get("DAQ"); return {first,last,mid,street,city,state,zip,dob,exp,license:lic}; }
function applyIdToForm(d){ if(!d) return; if(d.first&&!fv("c_first")) setVal("c_first",d.first); if(d.last&&!fv("c_last")) setVal("c_last",d.last); if(d.street) setVal("c_addr",d.street); if(d.city) setVal("c_addr_city",d.city); if(d.state) setVal("c_addr_state",d.state); if(d.zip) setVal("c_addr_zip",d.zip); window._idScan={license:d.license,dob:d.dob,exp:d.exp,state:d.state,at:todayISO()}; const s=document.getElementById("c_idstatus"); if(s) s.innerHTML=`<span class="tag ok">ID read — ${esc(d.first)} ${esc(d.last)}${d.state?" · "+esc(d.state):""}${d.license?" · "+esc(d.license.slice(-4).padStart(d.license.length,"•")):""}</span>`; toast("ID details filled in — check them before saving"); }
function setVal(id,v){ const e=document.getElementById(id); if(e) e.value=v; }
function idFromText(){ const t=fv("c_idraw"); const d=parseAAMVA(t); if(!d) return toast("That doesn't look like a driver's-license barcode. Scan the PDF417 barcode on the back of the ID.",true); applyIdToForm(d); }
function loadScriptOnce(url){ return new Promise((res,rej)=>{ if(document.querySelector(`script[src="${url}"]`)&&window.ZXing) return res(); const s=document.createElement("script"); s.src=url; s.onload=()=>res(); s.onerror=()=>rej(new Error("load failed")); document.head.appendChild(s); }); }
async function scanIdCameraZX(){ /* fallback decoder for browsers without native PDF417 (iPhone / iPad Safari, Firefox) */ const box=document.getElementById("c_idcam"); if(!box) return; box.innerHTML=`<div class="muted small">Loading the barcode reader…</div>`; try{ await loadScriptOnce("https://cdn.jsdelivr.net/npm/@zxing/library@0.18.6/umd/index.min.js"); }catch(e){ box.innerHTML=""; return toast("Couldn't load the barcode reader (offline?). Use a handheld scanner into the box, or add the ID photo.",true); } if(!window.ZXing||!ZXing.BrowserPDF417Reader){ box.innerHTML=""; return toast("Barcode reader unavailable on this browser — use a handheld scanner or the ID photo.",true); } const reader=new ZXing.BrowserPDF417Reader(); const video=document.createElement("video"); video.setAttribute("playsinline",""); video.style.width="100%"; video.style.borderRadius="8px"; box.innerHTML=""; box.appendChild(video); const hint=document.createElement("div"); hint.className="muted small"; hint.textContent="Hold the back of the license flat and fill the frame with the barcode."; box.appendChild(hint); const stop=()=>{ try{ reader.reset(); }catch(e){} if(box) box.innerHTML=""; }; window._idStop=stop; const t0=Date.now(); try{ await reader.decodeFromVideoDevice(undefined,video,(result,err)=>{ if(result){ const d=parseAAMVA(result.getText()); if(d){ applyIdToForm(d); stop(); return; } } if(Date.now()-t0>60000) stop(); }); }catch(e){ stop(); toast("Camera not available.",true); } }
async function scanIdCamera(){ let native=false; if("BarcodeDetector" in window){ try{ const formats=await BarcodeDetector.getSupportedFormats(); native=formats.includes("pdf417"); }catch(e){} } if(!native) return scanIdCameraZX(); const det=new BarcodeDetector({formats:["pdf417"]}); const video=document.createElement("video"); video.setAttribute("playsinline",""); let stream; try{ stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:"environment"}}); }catch(e){ return toast("Camera not available.",true); } video.srcObject=stream; await video.play(); const box=document.getElementById("c_idcam"); box.innerHTML=""; box.appendChild(video); video.style.width="100%"; video.style.borderRadius="8px"; const stop=()=>{ try{ stream.getTracks().forEach(t=>t.stop()); }catch(e){} box.innerHTML=""; }; window._idStop=stop; const t0=Date.now(); const tick=async()=>{ if(!document.getElementById("c_idcam")||Date.now()-t0>45000){ stop(); return; } try{ const codes=await det.detect(video); if(codes&&codes.length){ const d=parseAAMVA(codes[0].rawValue); if(d){ applyIdToForm(d); stop(); return; } } }catch(e){} requestAnimationFrame(tick); }; tick(); }
function addIdPhotoBack(input){ const f=input.files&&input.files[0]; if(!f) return; shrinkImage(f,900,0.7).then(data=>{ window._idPhotoBack=data; const s=document.getElementById("c_idprevb"); if(s) s.innerHTML=`<img src="${data}" style="max-height:110px;border-radius:8px;border:1px solid var(--line)">`; input.value=""; }); }
function addIdPhoto(input){ const f=input.files&&input.files[0]; if(!f) return; shrinkImage(f,900,0.7).then(data=>{ window._idPhoto=data; const s=document.getElementById("c_idprev"); if(s) s.innerHTML=`<img src="${data}" style="max-height:120px;border-radius:8px;border:1px solid var(--line)">`; input.value=""; }); }
function idPanelHTML(c){ return `<details style="margin:8px 0"${c.idPhoto||c.idScan?" open":""}><summary class="muted small" style="cursor:pointer">ID on file (optional) — scan the license barcode to fill the form, and/or keep a photo of the ID</summary><div class="grid g2" style="margin-top:8px"><div><div class="row" style="gap:6px"><button class="btn s" type="button" onclick="scanIdCamera()">Scan barcode with camera</button><span class="muted small">or scan with a handheld into the box:</span></div><input type="text" id="c_idraw" placeholder="handheld scanner output" style="margin-top:6px" onchange="idFromText()" onkeydown="if(event.key==='Enter'){event.preventDefault();idFromText()}"><div id="c_idcam" style="margin-top:6px"></div><div id="c_idstatus" style="margin-top:4px">${c.idScan?`<span class="tag ok">ID read ${dateShort(c.idScan.at)}${c.idScan.state?" · "+esc(c.idScan.state):""}</span>`:""}</div></div><div><label class="f">Photo of ID — front</label><input type="file" accept="image/*" capture="environment" onchange="addIdPhoto(this)"><div id="c_idprev" style="margin-top:6px">${c.idPhoto?`<img src="${c.idPhoto}" style="max-height:110px;border-radius:8px;border:1px solid var(--line)">`:""}</div>${c.idPhoto?`<a href="#" class="small" onclick="window._idPhoto='';document.getElementById('c_idprev').innerHTML='';return false">Remove</a>`:""}<label class="f" style="margin-top:8px">Photo of ID — back</label><input type="file" accept="image/*" capture="environment" onchange="addIdPhotoBack(this)"><div id="c_idprevb" style="margin-top:6px">${c.idPhotoBack?`<img src="${c.idPhotoBack}" style="max-height:110px;border-radius:8px;border:1px solid var(--line)">`:""}</div>${c.idPhotoBack?`<a href="#" class="small" onclick="window._idPhotoBack='';document.getElementById('c_idprevb').innerHTML='';return false">Remove</a>`:""}</div></div></details>`; }
